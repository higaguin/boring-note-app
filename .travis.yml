# language: node_js

# sudo: required
# services:
#   - docker

# before_install:
#   - wget -qO- https://toolbelt.heroku.com/install.sh | sh
#   - docker build -t higaguin/react-test -f ./client/Dockerfile.dev ./client

# script:
#   - docker run -e CI=true higaguin/react-test npm test
jobs:
  include:
    - stage: deploy to heroku
      language: node_js
      before_install: cd server
      deploy:
        provider: heroku
        api_key: $HEROKU_API_KEY
        app: boring-note-app-api
        on:
          branch: master
    - stage: deply to netlify
      provider: netlify
      site: cranky-franklin-0be987
      auth: f2013e0c-2ac7-4a0f-b5ed-f002a9413603
      edge: true
